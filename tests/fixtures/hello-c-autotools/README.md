# hello-c-autotools (stub)

Planned: a minimal autotools project — `hello.c`, `configure.ac`,
`Makefile.am` — to exercise the bootstrap worker against an
autotools build system.

Workers will produce `debian/` with `dh $@`, no special sequences,
`Architecture: any`, single binary.

TODO: populate with real upstream source + `expected/debian/`.
