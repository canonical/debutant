# hello-go (stub)

Planned: a minimal Go module (`go.mod`, `main.go`) to exercise the
bootstrap worker against the Go build system.

Workers will produce `debian/` with `dh-sequence-golang` (or
`dh-golang`), `Architecture: any`, single binary.

TODO: populate with real upstream source + `expected/debian/`.
